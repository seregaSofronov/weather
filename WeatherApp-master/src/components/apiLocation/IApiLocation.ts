export interface IApiLocation {
    activeSearch: any;
    activeFlagFunc: any;
    activeFlag: boolean;
    activeCity: string;
}

export interface ILocationState {
    city: any;
    cities: any;
    activeList: boolean;
}